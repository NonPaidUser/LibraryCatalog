{% extends 'base.html.twig' %}

{% block title %}{{ page_title }}{% endblock %}

{% block body %}
    <a href="{{ path('app_home') }}">🏠 Go to Home Page</a>

    <h1>{{ page_title }}</h1>

    <p class="return_borrow1">You have successfully borrowed: <strong>{{ book_title }}</strong> by <strong>{{ user_name }}</strong></p>

    <a href="{{ path('app_books') }}">⬅ Back to catalog</a>
{% endblock %}
